-- CreateTable
CREATE TABLE "Docktor" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "spaclist" TEXT NOT NULL,
    "aderess" TEXT NOT NULL,
    "startTime" TEXT NOT NULL,
    "endTime" TEXT NOT NULL,

    CONSTRAINT "Docktor_pkey" PRIMARY KEY ("id")
);
